apiVersion: batch/v1
kind: Job
metadata:
  name: dbt-transform
spec:
  template:
    spec:
      containers:
        - name: dbt-transform
          image: habtech-dbt:latest
          imagePullPolicy: Never
          command: ["dbt", "run"]
      restartPolicy: Never
  backoffLimit: 1
